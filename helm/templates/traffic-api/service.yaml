apiVersion: v1
kind: Service
metadata:
  name: traffic-api
  labels:
    {{- include "traffic-system.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: traffic-api
  ports:
    - port: 80
      targetPort: {{ .Values.trafficApi.port }}
      protocol: TCP
