apiVersion: apps/v1
kind: Deployment
metadata:
  name: traffic-alert-engine
  namespace: {{ .Values.global.namespace | default "trafficOS" }}
  labels:
    {{- include "trafficOS.labels" (merge (dict "component" "traffic-alert-engine") .) | nindent 4 }}
spec:
  replicas: {{ .Values.trafficAlertEngine.replicaCount }}
  selector:
    matchLabels:
      {{- include "trafficOS.selectorLabels" (merge (dict "component" "traffic-alert-engine") .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "trafficOS.selectorLabels" (merge (dict "component" "traffic-alert-engine") .) | nindent 8 }}
    spec:
      initContainers:
        - name: wait-for-kafka
          image: busybox:1.36
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              echo "Waiting for kafka:9092..."
              until nc -z kafka 9092; do
                echo "kafka not ready, sleeping 2s"
                sleep 2
              done
              echo "kafka is ready"
        - name: wait-for-clickhouse
          image: busybox:1.36
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              echo "Waiting for clickhouse:8123..."
              until nc -z clickhouse 8123; do
                echo "clickhouse not ready, sleeping 2s"
                sleep 2
              done
              echo "clickhouse is ready"
      containers:
        - name: traffic-alert-engine
          image: "{{ .Values.global.imageRegistry }}{{ .Values.trafficAlertEngine.image }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: actuator
              containerPort: {{ .Values.trafficAlertEngine.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: trafficOS-config
          env:
            - name: JAVA_OPTS
              value: {{ .Values.trafficAlertEngine.env.JAVA_OPTS | quote }}
            - name: KAFKA_INPUT_TOPIC
              value: {{ .Values.trafficAlertEngine.env.KAFKA_INPUT_TOPIC | quote }}
            - name: KAFKA_OUTPUT_TOPIC
              value: {{ .Values.trafficAlertEngine.env.KAFKA_OUTPUT_TOPIC | quote }}
            - name: ALERT_WEBHOOK_URL
              value: {{ .Values.trafficAlertEngine.webhookUrl | quote }}
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.trafficAlertEngine.port }}
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.trafficAlertEngine.port }}
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 4
          resources:
            {{- toYaml .Values.trafficAlertEngine.resources | nindent 12 }}
